{
  "name": "kblocks",
  "private": true,
  "scripts": {
    "watch": "cd packages/@kblocks/cli && npx tsc --watch",
    "dev": "skaffold run --tail",
    "build": "npm run --if-present -ws build"
  },
  "workspaces": [
    "packages/**/*",
    "test/wing-k8s"
  ]
}
