{
  "name": "kblocks",
  "private": true,
  "scripts": {
    "watch": "cd packages/@kblocks/block && npx tsc --watch",
    "dev": "skaffold run --port-forward --tail",
    "start": "skaffold run --port-forward",
    "build": "npm run --if-present -ws build",
    "test": "./test/run.sh"
  },
  "workspaces": [
    "packages/**/*",
    "scripts/bump-pack",
    "test/test-resource",
    "test/tests"
  ]
}
