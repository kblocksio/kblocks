apiVersion: kblocks.io/v1
kind: Block
spec:
  engine: noop
  definition:
    readme: ./README.md
    schema: ./src/values.schema.json
    description: An example of a noop block
    group: core
    version: v1
    kind: Secret
    plural: secrets
    singular: secret
  operator:
    namespace: kblocks-test
    env:
      PORTAL_REDIS_URL: "test-redis.kblocks-test.svc.cluster.local"
      PORTAL_REDIS_PASSWORD: "pass1234"
      KBLOCKS_SYSTEM_ID: test-system
    skipCrd: true
    flushOnly: true
metadata:
  name: secrets.core
